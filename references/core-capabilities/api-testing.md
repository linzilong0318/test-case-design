# 接口测试

> 本文档定义接口测试的专项测试维度和策略要点。与其他测试类型互补使用。

---

## 目录

| 行号 | 章节 |
|------|------|
| 22 | 一、功能测试 |
| 34 | 二、状态码验证 |
| 51 | 三、数据校验 |
| 61 | 四、认证授权 |
| 70 | 五、安全测试 |
| 85 | 六、错误处理 |
| 95 | 七、分页排序 |
| 104 | 八、搜索过滤 |

---

## 一、功能测试

| 测试场景 | 测试要点 |
|---------|---------|
| GET请求 | 正常查询单条/列表数据；带参数查询；参数为空/类型错误处理 |
| POST请求 | 正常创建资源；必填字段校验；字段格式校验；重复创建处理 |
| PUT/PATCH请求 | 完整更新/部分更新；更新不存在资源；并发更新处理 |
| DELETE请求 | 正常删除；删除不存在资源；级联删除；软删除 |
| 批量操作 | 批量创建/更新/删除；部分成功处理；事务回滚 |

---

## 二、状态码验证

| 状态码 | 说明 |
|-------|------|
| 200 OK | 正常响应 |
| 201 Created | 创建成功 |
| 204 No Content | 删除成功 |
| 400 Bad Request | 参数错误 |
| 401 Unauthorized | 未认证 |
| 403 Forbidden | 无权限 |
| 404 Not Found | 资源不存在 |
| 409 Conflict | 资源冲突 |
| 422 Unprocessable | 校验失败 |
| 500 Internal Error | 服务器错误 |

---

## 三、数据校验

| 测试场景 | 测试要点 |
|---------|---------|
| 字段校验 | 必填字段、类型、长度、格式、范围验证 |
| 业务规则 | 唯一性约束、外键约束、状态流转规则 |
| 数据一致性 | 数据库写入验证、缓存同步验证 |

---

## 四、认证授权

| 测试场景 | 测试要点 |
|---------|---------|
| Token认证 | Token生成、刷新、过期、撤销 |
| 权限控制 | 角色权限、资源权限、数据权限验证 |

---

## 五、安全测试

| 测试场景 | 测试要点 |
|---------|---------|
| 注入攻击 | SQL注入（参数化查询验证）、NoSQL注入、命令注入 |
| 越权访问 | 水平越权（IDOR，修改资源ID访问他人数据）、垂直越权（普通Token访问管理接口） |
| 批量赋值 | 请求体中添加只读字段（如 role、isAdmin、balance），验证是否被拒绝 |
| API限流 | 短时间内大量请求（>100次/秒），验证是否返回 429 |
| 敏感信息泄露 | 响应中是否返回密码哈希、Token、内部IP等敏感字段；错误堆栈是否暴露框架版本 |
| 安全头检查 | X-Content-Type-Options、X-Frame-Options、CSP、HSTS 等安全响应头 |
| JWT安全 | 无签名Token（alg=none）、过期Token、篡改Payload、密钥爆破 |
| 参数污染 | 重复参数（?id=1&id=2）、数组注入（?id[]=1&id[]=2）、HTTP Parameter Pollution |

---

## 六、错误处理

| 测试场景 | 测试要点 |
|---------|---------|
| 错误响应 | 错误码规范；错误信息清晰 |
| 异常处理 | 数据库异常、第三方服务异常、网络异常、超时处理 |
| 重试机制 | 自动重试；重试次数限制；幂等性保证 |

---

## 七、分页排序

| 测试场景 | 测试要点 |
|---------|---------|
| 分页 | 页码参数、每页数量、总数量、越界处理 |
| 排序 | 单字段/多字段排序；排序方向；默认排序 |

---

## 八、搜索过滤

| 测试场景 | 测试要点 |
|---------|---------|
| 搜索 | 精确搜索、模糊搜索、全文搜索、多条件组合 |
| 过滤 | 时间范围、状态、分类、自定义过滤 |
